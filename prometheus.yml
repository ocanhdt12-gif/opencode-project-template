global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: "opencode-monitor"

scrape_configs:
  - job_name: "nodejs"
    static_configs:
      - targets: ["localhost:3000"]
    metrics_path: "/metrics"
    scrape_interval: 5s
